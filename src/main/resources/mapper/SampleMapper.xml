<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.template.mapper.SampleMapper">

    <select id="findAll" parameterType="map" resultType="com.example.template.common.map.CamelCaseMap">
        SELECT id, email, password, name, role, created_at, updated_at FROM sample
        <where>
            <if test="keyword != null and keyword != ''">
                <choose>
                    <when test="searchType == 'name'">
                        AND name LIKE CONCAT('%', #{keyword}, '%')
                    </when>
                    <when test="searchType == 'email'">
                        AND email LIKE CONCAT('%', #{keyword}, '%')
                    </when>
                    <when test="searchType == 'all'">
                         AND (name LIKE CONCAT('%', #{keyword}, '%') OR email LIKE CONCAT('%', #{keyword}, '%'))
                    </when>
                </choose>
            </if>
        </where>
        ORDER BY id DESC
        <if test="limit != null and offset != null">
            LIMIT #{limit} OFFSET #{offset}
        </if>
    </select>

    <select id="count" parameterType="map" resultType="int">
        SELECT count(*) FROM sample
        <where>
            <if test="keyword != null and keyword != ''">
                <choose>
                    <when test="searchType == 'name'">
                        AND name LIKE CONCAT('%', #{keyword}, '%')
                    </when>
                    <when test="searchType == 'email'">
                        AND email LIKE CONCAT('%', #{keyword}, '%')
                    </when>
                    <when test="searchType == 'all'">
                         AND (name LIKE CONCAT('%', #{keyword}, '%') OR email LIKE CONCAT('%', #{keyword}, '%'))
                    </when>
                </choose>
            </if>
        </where>
    </select>

    <select id="findById" parameterType="long" resultType="com.example.template.common.map.CamelCaseMap">
        SELECT id, email, password, name, role, created_at, updated_at FROM sample WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="map">
        INSERT INTO sample (email, password, name, role)
        VALUES (#{email}, #{password}, #{name}, #{role})
    </insert>

    <update id="update" parameterType="map">
        UPDATE sample
        SET email = #{email},
            password = #{password},
            name = #{name},
            role = #{role},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM sample WHERE id = #{id}
    </delete>

    <select id="findByEmail" parameterType="string" resultType="com.example.template.common.map.CamelCaseMap">
        SELECT id, email, password, name, role FROM sample WHERE email = #{email}
    </select>

</mapper>
